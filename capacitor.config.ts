import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.clearyourbacklog',
  appName: 'Clear Your Backlog',
  webDir: 'dist'
};

export default config;
