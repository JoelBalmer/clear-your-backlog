import { libraryOutline } from 'ionicons/icons';
import PlaceholderPage from '../components/PlaceholderPage';

const Library: React.FC = () => (
  <PlaceholderPage
    title="Library"
    icon={libraryOutline}
    headline="Your library is empty"
    body="Search a game and rate it to start filling your shelves. Coming in Phase 4."
  />
);

export default Library;
